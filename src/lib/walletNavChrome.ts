/** Shared nav wallet chrome — keep loading + connected triggers identical radius/px. */
export const walletNavRadius =
  "rounded-[var(--privy-border-radius-md,0.5rem)]";
export const walletNavPad = "px-2 py-2";
/** Loading placeholder only — slightly taller so text doesn’t feel tight. */
export const walletNavLoadingPad = "px-2 py-2.5";
