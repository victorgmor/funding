version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm ci
      - npm run build
run:
  command: npm start
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production
    - name: HOST
      value: "0.0.0.0"
